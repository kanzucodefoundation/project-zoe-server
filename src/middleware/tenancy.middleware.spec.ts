import { TenancyMiddleware } from "./tenancy.middleware";

describe("TenancyMiddleware", () => {
  it("should be defined", () => {
    //expect(new TenancyMiddleware()).toBeDefined(); @TODO Update this
  });
});
